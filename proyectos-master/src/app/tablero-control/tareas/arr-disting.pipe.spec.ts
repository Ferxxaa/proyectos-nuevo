import { ArrDistingPipe } from './arr-disting.pipe';

describe('ArrDistingPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrDistingPipe();
    expect(pipe).toBeTruthy();
  });
});
