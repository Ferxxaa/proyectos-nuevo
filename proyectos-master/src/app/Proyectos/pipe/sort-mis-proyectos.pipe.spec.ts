import { SortMisProyectosPipe } from './sort-mis-proyectos.pipe';

describe('SortMisProyectosPipe', () => {
  it('create an instance', () => {
    const pipe = new SortMisProyectosPipe();
    expect(pipe).toBeTruthy();
  });
});
