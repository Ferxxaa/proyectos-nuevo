import { CleanDuplicadosPipe } from './clean-duplicados.pipe';

describe('CleanDuplicadosPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanDuplicadosPipe();
    expect(pipe).toBeTruthy();
  });
});
