import { IdEtapaToNombrePipe } from './id-etapa-to-nombre.pipe';

describe('IdEtapaToNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new IdEtapaToNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
