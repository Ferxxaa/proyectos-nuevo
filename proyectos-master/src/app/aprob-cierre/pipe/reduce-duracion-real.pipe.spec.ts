import { ReduceDuracionRealPipe } from './reduce-duracion-real.pipe';

describe('ReduceDuracionRealPipe', () => {
  it('create an instance', () => {
    const pipe = new ReduceDuracionRealPipe();
    expect(pipe).toBeTruthy();
  });
});
