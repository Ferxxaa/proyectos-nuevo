import { ReduceDuracionProgramadaPipe } from './reduce-duracion-programada.pipe';

describe('ReduceDuracionProgramadaPipe', () => {
  it('create an instance', () => {
    const pipe = new ReduceDuracionProgramadaPipe();
    expect(pipe).toBeTruthy();
  });
});
