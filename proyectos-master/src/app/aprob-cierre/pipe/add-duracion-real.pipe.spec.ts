import { AddDuracionRealPipe } from './add-duracion-real.pipe';

describe('AddDuracionRealPipe', () => {
  it('create an instance', () => {
    const pipe = new AddDuracionRealPipe();
    expect(pipe).toBeTruthy();
  });
});
