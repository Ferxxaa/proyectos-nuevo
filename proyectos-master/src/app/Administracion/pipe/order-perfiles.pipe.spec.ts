import { OrderPerfilesPipe } from './order-perfiles.pipe';

describe('OrderPerfilesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderPerfilesPipe();
    expect(pipe).toBeTruthy();
  });
});
