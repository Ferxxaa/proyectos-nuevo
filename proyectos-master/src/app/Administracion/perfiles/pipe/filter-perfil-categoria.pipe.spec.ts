import { FilterPerfilCategoriaPipe } from './filter-perfil-categoria.pipe';

describe('FilterPerfilCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPerfilCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
